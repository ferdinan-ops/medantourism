import { Text, View } from 'react-native'
import COLORS from '../../theme/colors'

const HotelPage = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: COLORS.black1 }}>Hotel Page</Text>
    </View>
  )
}

export default HotelPage
