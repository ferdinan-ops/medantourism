import { Text, View } from 'react-native'
import COLORS from '../../theme/colors'

const StasiunPage = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: COLORS.black1 }}>Stasiun Page</Text>
    </View>
  )
}

export default StasiunPage
