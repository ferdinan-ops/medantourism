import { Text, View } from 'react-native'
import COLORS from '../../theme/colors'

const RekreasiPage = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: COLORS.black1 }}>Rekreasi Page</Text>
    </View>
  )
}

export default RekreasiPage
